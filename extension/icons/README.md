# icons/

Icon files are not included in v0.1.0 baseline.

Planned sizes: 16×16, 32×32, 48×48, 128×128 (PNG).

Add icons in a future phase and reference them in `manifest.json` under the `icons` and `action.default_icon` fields.
